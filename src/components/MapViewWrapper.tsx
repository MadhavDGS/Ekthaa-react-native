/**
 * MapView Wrapper - Native Version
 * Exports react-native-maps components
 */

import MapView, { Marker } from 'react-native-maps';

export { MapView, Marker };
export const isMapSupported = true;
